import React, { Fragment } from 'react';

const AllQuiz = ({quiz}) => {
    const {question, options, correctAnswer} = quiz;
    return (
      <div>
        
      </div>
    );
};

export default AllQuiz;